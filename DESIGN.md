# Potion UI 설계

## 목적

모바일에서 사용자가 **집에 있는 술로 지금 만들 수 있는 한 잔과 안주**를 빠르게 고른다. 화면마다 주요 행동은 하나만 둔다.

## 톤과 토큰

친근하고 차분하다. 술을 잘 아는 사람처럼 말하지 않고, 선택을 도와주는 말투를 쓴다.

| 용도 | 값 |
|---|---|
| Primary / 주요 CTA | `#E06D53` |
| Secondary / 따뜻한 강조 | `#F4A261` |
| Background | `#FAF9F6` |
| Accent / 선택·가능 상태 | `#2A9D8F` |
| Text | `#2D2926` |
| Border | `#E8E1DA` |

- `Pretendard Variable` 웹폰트를 사용하고, `system-ui, sans-serif`를 폴백으로 둔다.
- 여백은 8px 단위, 화면 좌우는 20px, 버튼과 입력 영역은 최소 44px 높이로 한다.
- 모서리는 12px만 사용한다. 그림자는 거의 쓰지 않고, 구분은 배경색과 1px 테두리로 만든다.
- 배경색은 넓게 쓰되 카드까지 모두 크림색으로 겹치지 않는다. 흰색 표면과 테라코타 CTA로 위계를 만든다.
- 그라데이션, 이탤릭 세리프 제목, 장식용 알약, 중첩 카드, 의미 없는 아이콘 타일은 사용하지 않는다.

## 화면 구조

### 1. 연령 안내

- 최초 1회만 전면 안내: `만 19세 이상만 이용할 수 있어요.`
- CTA: `확인했어요`
- 하단 고지: `과도한 음주는 건강에 해롭습니다.`

### 2. 홈

- 상단: `Potion`과 즐겨찾기 아이콘
- 인사: `오늘 당신의 기분은 어떤 맛인가요?`
- 가장 큰 CTA: `집에 있는 술로 찾기`
- 보조 진입: 상황 칩 `퇴근 후`, `가벼운 반주`, `주말 홈파티`, `저도수`
- 아래에는 작은 가로 목록만 둔다: `5분 완성`, `남은 와인 활용`, `편의점 재료`
- CTA를 누르면 재료 선택으로, 상황 칩을 누르면 해당 필터 결과로 이동한다.

### 3. 보유 술 선택

- 제목: `어떤 술이 있나요?`
- 큰 분류를 먼저 보여준다: `6대 기주`, `와인`, `한국술`, `일본술`.
- 선택한 분류 안에서 세부 종류를 체크 칩으로 고른다. 예: `위스키`, `막걸리`, `사케`.
- 선택 사항 섹션: `함께 있는 재료`에서 `탄산수`, `콜라`, `토닉워터`, `주스` 등을 고른다.
- 하단 고정 CTA: `N개 레시피 보기`. 선택이 없으면 비활성화한다.

### 4. 추천 결과

- 제목: `지금 만들 수 있어요`와 결과 수
- 필터는 한 줄 가로 스크롤: `당도`, `도수`, `상황`, `주종`.
- 첫 목록: **바로 만들 수 있음**. 카드에는 이름, 맛 2개, 예상 도수, 시간, 난이도만 표시한다.
- 두 번째 목록: **한두 가지만 더하면 돼요**. 부족 재료를 카드에 한 줄로 표시한다.
- 결과가 없으면: `딱 맞는 조합은 아직 없어요. 재료 하나만 더 골라볼까요?`와 `재료 다시 고르기` CTA를 보여준다.

### 5. 레시피 상세

- 상단: 뒤로가기, 즐겨찾기
- 이름과 한 줄 설명, 맛·도수·시간·난이도를 한 줄로 표시한다.
- `재료`는 1잔 기준의 수량과 단위, 대체 재료를 함께 보여준다.
- `만드는 순서`는 번호·짧은 문장·필요한 도구만 쓴다.
- `잘 어울리는 안주`는 편의점·배달·간단 조리 탭으로 나누고, 각 안주에 한 줄의 페어링 이유를 적는다.
- 하단 고정 CTA: `만들어 볼게요`. 누르면 짧은 완료 피드백과 즐겨찾기 제안을 보여준다.

### 6. 즐겨찾기

- 기기 내 저장 목록만 제공한다.
- 빈 상태: `오늘 하루도 수고 많았어요. 가벼운 한 잔을 찾아볼까요?`
- CTA: `레시피 찾기`

## 컴포넌트와 상태

- 버튼: Primary(테라코타 채움), Secondary(테두리), Text만 사용한다.
- 칩: 미선택은 흰색 테두리, 선택은 옅은 Teal 배경과 체크 표시를 사용한다.
- 레시피 카드는 한 화면에 하나의 평면 카드만 쓴다. 전체 카드를 눌러 상세로 이동한다.
- 이미지는 선택사항이다. 이미지가 없으면 카테고리별 단색 표면과 레시피명을 사용한다.
- 로딩은 스켈레톤 3개, 오류는 `다시 시도`, 빈 결과는 재료 변경 행동으로 끝낸다.

## 인터랙션과 접근성

- 탭·칩·버튼에는 즉시 선택 상태와 텍스트 피드백을 제공한다.
- 전환은 150~200ms의 opacity·translate만 사용한다. 자동 재생, 튀는 스케일, 장식 애니메이션은 넣지 않는다.
- 색만으로 제조 가능 상태를 구분하지 않는다. `바로 가능`, `재료 1개 필요` 텍스트를 함께 쓴다.
- 포커스 표시, 44px 터치 영역, 이미지 대체 텍스트, 충분한 명도 대비를 기본으로 한다.

## 구현 우선순위

1. 연령 안내, 홈, 재료 선택, 결과, 상세
2. 빈·오류·로딩 상태와 기기 내 즐겨찾기
3. 이미지, 공유, 로그인 등은 실제 필요가 확인될 때 추가
